import { Dictionary } from "./dictionary.model";

export class Student {
    name: string;
    dateOfBirth: number;
    identityCard: string;
    address: string;
    class: Dictionary;
    isAgreed: boolean;
}
